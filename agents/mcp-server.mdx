---
title: "Connect the Hue MCP server"
sidebarTitle: "Connect MCP"
description: "Connect an MCP client to Hue with browser sign-in or a project key, with setup instructions for Claude Code, Codex, Cursor and Conductor."
---

The Hue project-data MCP server is a hosted [Model Context Protocol](https://modelcontextprotocol.io) server at `https://mcp.hue.run/mcp`. There is nothing to install. Browser sign-in grants **Read and write** access to all current and future active projects in one organization. A project key stays limited to one project, with **Read** or **Read and write** access. Billing and plan changes stay in Settings. `https://docs.hue.run/mcp` is a separate documentation-only server and cannot read your project.

## Quick start

1. Choose your client below and add `https://mcp.hue.run/mcp`. For **sign-in with Hue**, leave out API keys and custom headers.
2. When the client opens Hue, sign in with your verified account, check the organization and choose **Allow**. You must be an actual member of that organization. The approval grants **Read and write** across its active projects; choose the organization if Hue offers more than one.
3. Refresh the client's MCP status or start a new session, then [verify the connection](#verify-the-connection) by reading a trace through the agent.

Already have a project key, or using a client without sign-in support? [Prepare the key](#connect-with-a-project-key), then use your client's key instructions below. A **Tracing only** key cannot connect to this MCP server.

## Connect your client

Start with **Any MCP client** for the connection settings, or choose your client for exact commands.

<Tabs>
  <Tab title="Any MCP client">
    In your client's MCP settings, add a **remote server** with these values. The client must support **Streamable HTTP** and **OAuth** for browser sign-in.

    | Setting | Value |
    | --- | --- |
    | Name | `hue` |
    | Server URL | `https://mcp.hue.run/mcp` |
    | Transport | Streamable HTTP |
    | Authentication | OAuth / sign in |

    Save the server and choose its connect or authenticate action. Hue opens in your browser: sign in, check the organization and choose **Allow** for **Read and write** across its active projects. Leave custom headers and API-key fields empty; the client handles OAuth discovery and tokens. Return to the client and [verify the connection](#verify-the-connection).

    Setting names vary by app. Use the tabs for exact Claude Code, Codex, Cursor and Conductor instructions. Hosted connectors can use the same URL and sign-in flow when their plan and workspace settings allow custom MCP servers; Hue has not yet verified claude.ai, Claude Desktop Connectors or ChatGPT end to end.

    <Accordion title="Use a project key instead">
      If your client supports bearer authentication or custom headers, [create a Read or Read and write key](#connect-with-a-project-key) and configure `Authorization: Bearer <project key>` using the client's secret storage or environment-variable reference. Select key or bearer authentication instead of OAuth. Never put the key in the server URL or shared configuration. Environment-reference syntax depends on the client.
    </Accordion>
  </Tab>
  <Tab title="Claude Code">
    Add the server, then run `/mcp` in Claude Code, select `hue` and choose **Authenticate**. This adds `hue` for the current project; add `--scope user` for all projects. Until you authenticate, `claude mcp get hue` shows `! Needs authentication`, which is expected.

    ```sh
    claude mcp add --transport http hue https://mcp.hue.run/mcp
    ```

    To replace a key entry, first run `claude mcp remove hue -s <scope>` with the scope `claude mcp get hue` reports, then add the URL-only entry. A local entry overrides project and user entries, so check that an older entry is not taking precedence.

    <Accordion title="Use a project key instead">
      First [prepare the key](#connect-with-a-project-key), then configure this client.

      Add `hue` for all your projects. Keep the single quotes, and keep `--header` after the URL.

      ```sh
      claude mcp add --transport http --scope user hue https://mcp.hue.run/mcp \
        --header 'Authorization: Bearer ${HUE_MCP_KEY}'
      ```

      To share the server with a repository instead, add a project-scoped `.mcp.json` next to your application code. It holds only the `${HUE_MCP_KEY}` reference. Claude Code expands it from the environment it starts in and asks for approval before using a project-scoped server.

      ```json .mcp.json
      {
        "mcpServers": {
          "hue": {
            "type": "http",
            "url": "https://mcp.hue.run/mcp",
            "headers": {
              "Authorization": "Bearer ${HUE_MCP_KEY}"
            }
          }
        }
      }
      ```

      **Verify:** run `claude mcp get hue` and look for `Status: ✔ Connected`; the command exits successfully even when the connection fails. The `Headers` line must read `Authorization: Bearer ${HUE_MCP_KEY}`. Inside Claude Code, tools appear as `mcp__hue__<tool>`.

      <Accordion title="If the Claude Code check fails">
        With double quotes around the header, your shell writes the key itself, or just `Bearer` when the variable is unset, into the settings file. If the `Headers` line shows only `Bearer` or a key, run `claude mcp remove hue -s <scope>` with the scope it reports, add the server again with single quotes, and rotate a key that was stored or printed. Placed before the server name, `--header` swallows the name and URL, and Claude Code reports `error: missing required argument 'name'`. `claude mcp list` warns `Missing environment variables: HUE_MCP_KEY` when the variable is unset.

        Claude Code keeps one `hue` entry per scope, and a local entry overrides project and user entries. To switch between a key and [sign-in](#sign-in-with-hue-instead-of-a-key), run `claude mcp remove hue -s <scope>` with the scope `claude mcp get hue` reports, then add the other form. After you switch from sign-in to a key, new sessions can skip `hue` for about 15 minutes; run `/mcp` in a new session to confirm its tools are listed.
      </Accordion>
    </Accordion>
  </Tab>
  <Tab title="Codex">
    Add the server from a terminal where you can use a browser. `codex mcp add` may start sign-in immediately; `codex mcp login hue` starts or repeats sign-in:

    ```sh
    codex mcp add hue --url https://mcp.hue.run/mcp
    codex mcp login hue
    ```

    To replace a key entry, run `codex mcp remove hue` first, then add the URL-only entry above. Check the project's `.codex/config.toml` too if it overrides your user configuration.

    <Accordion title="Use a project key instead">
      First [prepare the key](#connect-with-a-project-key), then configure this client.

      Add the server for every project:

      ```sh
      codex mcp add hue --url https://mcp.hue.run/mcp --bearer-token-env-var HUE_MCP_KEY
      ```
      Codex reads the key from the environment variable each time it connects. The equivalent entry in `~/.codex/config.toml`, or in `.codex/config.toml` of a project Codex trusts:

      ```toml config.toml
      [mcp_servers.hue]
      url = "https://mcp.hue.run/mcp"
      bearer_token_env_var = "HUE_MCP_KEY"
      ```

      **Verify:** start `codex` and run `/mcp`; `hue` is listed with its tools. `codex mcp list` and `codex mcp get hue` only print the configuration and do not contact Hue.

      <Accordion title="If the Codex check fails">
        If Codex reports `Environment variable HUE_MCP_KEY for MCP server 'hue' is not set` or `… is empty`, export the key in the shell that starts Codex and restart it. For every key Hue refuses, Codex says only `The hue MCP server is not logged in`: run the [curl check](#verify-the-connection) to see Hue's reason, and don't run `codex mcp login hue`, which starts sign-in instead of fixing the key.
      </Accordion>
    </Accordion>
  </Tab>
  <Tab title="Cursor">
    <Warning>
      Cursor's native browser sign-in is currently blocked: Hue rejects its `cursor://anysphere.cursor-mcp/oauth/callback` redirect. Use a project key below until the compatibility fix is deployed, or use Claude Code or Codex for browser sign-in.
    </Warning>

    <Accordion title="Connect with a project key" defaultOpen>
      First [prepare the key](#connect-with-a-project-key), then configure this client.

      Add the server to `.cursor/mcp.json` in the repository, or to `~/.cursor/mcp.json` for all projects:

      ```json .cursor/mcp.json
      {
        "mcpServers": {
          "hue": {
            "url": "https://mcp.hue.run/mcp",
            "headers": {
              "Authorization": "Bearer ${env:HUE_MCP_KEY}"
            }
          }
        }
      }
      ```

      Cursor loads a new server only after you approve it: turn on **hue** in Cursor's MCP settings, or run `cursor-agent mcp enable hue` with the Cursor CLI. Check that `HUE_MCP_KEY` holds the key you intend before you approve.

      **Verify:** `cursor-agent mcp list` shows `hue: ready`, and `cursor-agent mcp list-tools hue` lists 93 tools for a **Read and write** key or 47 tools for a **Read** key.

      <Accordion title="If the Cursor check fails">
        The approval covers the expanded header, so Cursor asks again whenever `HUE_MCP_KEY` changes or becomes unset. If `HUE_MCP_KEY` is unset, Cursor sends the unexpanded reference without a warning, and Hue refuses it. For a key Hue refuses with `401`, Cursor shows only `requires_authentication` and suggests `agent mcp login hue`. Don't run it; run the [curl check](#verify-the-connection) to see Hue's reason.
      </Accordion>
    </Accordion>
  </Tab>
  <Tab title="Conductor">
    Conductor uses the MCP configuration of your selected agent. In a **local Conductor workspace**, run the command for that agent in its terminal:

    <CodeGroup>
    ```sh Claude Code
    claude mcp add --transport http --scope user hue https://mcp.hue.run/mcp
    ```
    ```sh Codex
    codex mcp add hue --url https://mcp.hue.run/mcp
    ```
    </CodeGroup>

    Open **MCP status** from the plug icon or `/mcp-status`, refresh, then use **hue**'s authentication action. Sign in to Hue, check the organization and choose **Allow** for **Read and write** across its active projects. Leave out API keys and custom headers for this connection. If an older `hue` entry uses a key, replace it using the **Claude Code** or **Codex** tab.

    Start a new agent session if its tool list has not updated, then [verify the connection](#verify-the-connection). These instructions cover local workspaces; a cloud workspace has a separate configuration and browser callback environment. See [Conductor's MCP setup guide](https://www.conductor.build/docs/guides/configure-mcp-servers#check-status-in-conductor).

    Hue's production Conductor browser flow is still awaiting end-to-end acceptance; a connected status alone is not a successful trace read.
  </Tab>
</Tabs>

## Sign in with Hue instead of a key

Sign-in uses OAuth and requires no project key. Your account must be verified and an actual member of the organization; any member can approve a connection, without an owner or admin role. If you were invited, accept the invitation in Hue first, then connect again. An organization can be connected before it has any projects. Administrative visibility without membership does not grant access to a connected app.

Each new approval grants **Read and write** access to all current and future active projects in that organization. Check the app, signed-in account and organization on the consent page, then choose **Allow** or **Deny**. The organization picker appears only when you have more than one eligible membership. To switch projects within the approved organization, use `list_projects` and pass the chosen `project_id` on subsequent tool calls; no new sign-in is needed.

Existing sign-in connections keep their original project and access level, including **Read**. Refreshing does not broaden them. To grant organization-wide access, disconnect the earlier connection in **Settings → Connected apps** and authenticate again. Project keys always remain limited to their original project.

Leave out the `Authorization` header and any bearer-token environment-variable setting when signing in. An old key entry can suppress sign-in or carry a failing header into authorization. Replace it using the instructions in your [client's tab](#connect-your-client).

<Accordion title="Access, expiry and disconnecting">
  - New sign-in connections always use **Read and write**, within your current organization role; `add_api_key` is never available. For credentials limited to reading one project, use a **Read** project key. Leave `?read_only=true` off the sign-in URL.
  - The client receives access tokens that last 10 minutes and refreshes them automatically. Your approval lasts 30 days from when you connect; refreshing never extends it, so sign in again after that.
  - The connection acts for you. Writes are attributed to you, and owner- or admin-only tools check your current role: revoking keys, changing project settings or trace checks, archiving eval sets and evaluators, registering managed targets and publishing the intent taxonomy. A connected app can never create project keys with `add_api_key`.
  - Sign-in covers the MCP server only. The SDKs and REST API still need a project key.
  - **Settings → Connected apps** shows **All projects** for an organization connection and the project name for an earlier project connection. You can disconnect your connections; owners and admins can see and disconnect everyone's in the organization. Resetting your password, signing out everywhere or leaving the organization ends your approvals. Archiving a project removes it from an organization connection's available projects; it ends an earlier connection scoped to that project.
</Accordion>

## Connect with a project key

1. An owner or admin creates a **Read** key, or a **Read and write** key if the agent should change project data, in **Settings → Integrations & API keys**. Save the key in your secret manager; it is shown once. Keep it separate from the application's **Tracing only** key.
2. For clients that read `HUE_MCP_KEY`, export the key alone, without `Bearer `, from your secret manager into the shell that starts the client. If your client offers a secure credential prompt, use that instead.
3. Follow your [client's tab](#connect-your-client), restart it from that shell and approve **hue** if asked. Desktop apps started from the Dock or a launcher do not inherit a terminal's variables.

Never put the key in application code, shared configuration or a prompt. See [project keys](/guides/project-keys) for access and rotation.

<Visibility for="agents">
  **If you are a coding agent:** reference `HUE_MCP_KEY` and never ask the user to paste the key. Adding configuration alone does not prove that the client connected. If the current session cannot load the server, use the [key check](#verify-the-connection), then ask the user to restart and [verify through the client](#verify-the-connection). For sign-in, let the user complete browser consent; do not request passwords or tokens.
</Visibility>

### Let your coding agent set it up

For a client that supports environment-variable references, paste this into your coding agent, started from a shell where `HUE_MCP_KEY` is exported. If your client uses a secure credential prompt, configure the key there instead.

```text Prompt wrap
Connect me to the Hue MCP server.

It is a remote MCP server (Streamable HTTP) at https://mcp.hue.run/mcp. Authenticate with a bearer token read from the HUE_MCP_KEY environment variable — reference the variable, do not write the key into a config file.

Register it under the name "hue" using whatever MCP configuration my client uses, then call get_project_context and tell me which Hue project it reached.
```

Most clients load a new server only when they start, and Claude Code and Cursor also ask you to approve it, so the agent usually can't call `get_project_context` in the session that added `hue`. Restart the client from the same shell, approve **hue** if asked, and ask again. If the agent asks you to paste the key, point it at `HUE_MCP_KEY` instead.

## Verify the connection

Ask the agent in the client you just connected:

```text
Use the Hue MCP: call list_projects and confirm which project to inspect. Then call get_project_context and show its 5 most recent error traces with links. For an organization connection, pass the project's id as project_id on each call after list_projects.
```

Then ask it to call `get_trace` for one of the returned trace IDs. If there are no error traces, ask `search_traces` for recent traces without an error filter. An empty result is not an authentication failure; use a project with a known test trace to verify the full read path.

A successful check names the intended project and returns that project's trace through the MCP tool calls. A saved configuration or a **Connected** status alone does not establish that those calls work.

`list_projects` returns the active projects this connection can reach, in pages of up to 100. Follow `next_cursor` until it is `null` to see them all. An organization connection must pass a returned UUID as `project_id` on every other tool call, including `get_project_context`, `search_traces` and `get_trace`. A project key or earlier project connection returns only its project and keeps its existing tool arguments without `project_id`.

`get_project_context` returns the selected project and what the credential can do. `can_write` is `true` when write tools are available; `read_only` reports only the `?read_only=true` URL flag, so a **Read** key shows `read_only: false` together with `can_write: false`, and `write_preset` names the access that writing needs, not the access you have. If it reports a different project than you expected, check the selected `project_id`, the project where the key was created, or the original project of an earlier sign-in connection.

To check a key without an MCP client, send one tool call from a shell where `HUE_MCP_KEY` is exported. This is the check to use when a client shows only a short status, and the non-interactive check for a coding agent. It proves the key, not the client's configuration:

```sh
printf 'Authorization: Bearer %s\n' "$HUE_MCP_KEY" | \
curl -sS -w '\nHTTP %{http_code}\n' -X POST https://mcp.hue.run/mcp \
  -H @- \
  -H "Content-Type: application/json" \
  -H "Accept: application/json, text/event-stream" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_project_context","arguments":{}}}'
```

The shell's `printf` builtin sends the header through standard input, keeping the expanded key out of curl's process arguments. `HTTP 200` with an `event: message` response that names your project means the key works. `401` means the key is missing, wrong, revoked or expired, and `403` means it is not a **Read** or **Read and write** key; the response body explains the refusal. The server needs both `Accept` types and answers `406` without them. It is stateless, so no session or `initialize` call is needed first.

## Troubleshooting

For key-based connections, the [curl check](#verify-the-connection) shows Hue's response when the client displays only a short status. Some clients treat any `401` as a request to sign in; fix or remove a failing key header before starting OAuth. For sign-in connections, use the client's authentication action and the sign-in rows below.

| Symptom | Cause | Fix |
| --- | --- | --- |
| Your organization is missing from the consent page | The signed-in account is unverified, has not accepted its invitation, or is not an actual member | Check the account shown on the page, verify your email and accept your invitation in Hue, then reconnect. Ask an organization owner or admin to check your membership. Staff or administrative visibility alone is not membership. Projects are selected through tools after approval. |
| A project is missing from `list_projects` | It is archived, belongs to another organization, or this is a project key or earlier project-scoped connection | Confirm the approved organization and that the project is active. Earlier sign-in connections keep their original project until you disconnect and authenticate again. A project key cannot switch projects. |
| A tool asks for `project_id` | An organization connection has not selected a project for this call | Call `list_projects`, choose the intended project's UUID and pass it as `project_id` on every other tool call. Use the tool schema returned by `tools/list`; project keys and earlier project connections keep their existing arguments. |
| New sign-in fails with `invalid_scope` | The client explicitly requests only `mcp:read` | New approvals require both `mcp:read` and `mcp:write`. Let the client use the scopes advertised by Hue, or use a **Read** project key for a credential limited to reads. Existing **Read** connections can still refresh. |
| The client reports missing OAuth metadata | The URL is wrong, a saved entry still targets an old endpoint, or discovery failed | Use `https://mcp.hue.run/mcp` without a custom authorization header, refresh the client's configuration and retry. If it persists, report the client/version and error text without tokens. |
| Consent succeeds but no Hue tools appear | The running agent session has not loaded the new connection | Refresh MCP status and start a new session, then run the verification above. In Conductor, check the configuration for the selected agent. |
| A Hue sign-in page opens, or the client shows **Needs authentication**, `Authentication required`, `requires_authentication` (Cursor), `The hue MCP server is not logged in` (Codex), although you configured a key | Hue refused the key. Usually `HUE_MCP_KEY` is unset or empty where the client started, the header lacks `Bearer ` or repeats it, the key is in another header such as `x-api-key`, or the key is wrong or revoked. Also check that the saved configuration still holds the `${HUE_MCP_KEY}` reference rather than `Bearer` or the key (a CLI `--header` in double quotes), that no other `hue` entry without the header overrides it (Claude Code), and that Codex was added with `--bearer-token-env-var` | Run the curl check and match its message to the rows below. Export `HUE_MCP_KEY` in the shell that starts the client, restart the client, and send exactly `Authorization: Bearer <key>`. Don't finish a sign-in that opened because of a failing key: it fails too. |
| `Malformed initial access token Authorization header` or `Dynamic Client Registration rejected (HTTP 401) … Invalid initial access token` | The client tried to sign in and sent your failing key header along: the variable is unset or empty or already holds `Bearer `, or the key is wrong, a placeholder or an unexpanded `${…}` reference | Fix the key as in the row above, then restart the client. Or remove the header and sign in. |
| Codex reports `Environment variable HUE_MCP_KEY for MCP server 'hue' is not set` or `… is empty` | The variable is missing, empty or set without `export` in the shell that started Codex | Export the key in that shell and restart Codex. |
| `401` `The bearer token is not a Hue credential` | The client sent something other than a key: the reference itself, such as `${HUE_MCP_KEY}`, because the variable was unset or the syntax is not the client's, or a value with extra characters such as surrounding quotes | Set the variable to the key alone before starting the client, and use the client's syntax: `${HUE_MCP_KEY}` for Claude Code and `${env:HUE_MCP_KEY}` for Cursor. For other clients, follow their environment-variable or secret-storage syntax. Codex needs `bearer_token_env_var` and does not expand `${…}` in `http_headers`. |
| `401` `A valid project service key is required` | The key is wrong, truncated, revoked or expired | Copy the key again from your secret manager, or create a new one in **Settings → Integrations & API keys**. |
| `403` `This key cannot read project data` | The key is **Tracing only** or a legacy **Tracing and evaluations** or **Source capture (legacy)** key | Create a **Read** or **Read and write** key and configure it in your client. Or [sign in with Hue](#sign-in-with-hue-instead-of-a-key). |
| `Unexpected content type: text/html`, HTTP `404` or `MCP endpoint not found` | Wrong URL: the host root, `/api` without `/mcp`, or `/mcp` on `app.hue.run` | Use `https://mcp.hue.run/mcp` exactly. A trailing slash is fine. |
| `429` `Too many failed authentications for this key`, or `Too many requests. Please try again later.` while a client tries to sign in | Ten failed attempts with this key within a minute, often from another client with an old or truncated copy, or repeated sign-in attempts from a client whose key header keeps failing | Fix or stop that client, then wait a minute. Until then the correct key is refused too. |
| `503` with `Retry-After: 1` | The server is momentarily busy | Retry after a second. |
| The client reports `invalid HTTP header` | The stored key ends with a newline | Store the key without a trailing newline. |
| Cursor shows `not loaded (needs approval)` or `has not been approved` | Cursor has not approved the server, or `HUE_MCP_KEY` changed or became unset since you approved it | Check that `HUE_MCP_KEY` is exported and holds the key you intend, then turn on **hue** in Cursor's MCP settings, or run `cursor-agent mcp enable hue`. Cursor asks again after every change. |
| A client you connected by signing in shows **Needs authentication** or `The hue MCP server is not logged in` | The approval ended: 30 days passed, someone disconnected it in **Settings → Connected apps**, you reset your password or signed out everywhere, or you left the organization. An earlier project-scoped approval also ends if its project is archived | Use your client’s authentication action to sign in again: `/mcp` → **Authenticate** in Claude Code or `codex mcp login hue` in Codex. For Cursor, use the project-key instructions in its tab until native sign-in is supported. |
| A tool call fails as an unknown tool | Write tools are hidden for **Read** access and for a URL with `?read_only=true`; [retired tool names](#retired-tool-names) are no longer served; a tool described in a development branch is not available until its platform release is deployed | Use a **Read and write** key without `?read_only=true`, reconnect an earlier **Read** sign-in connection if organization-wide writes are intended, or call the current tool name. Use the tools your connection's `tools/list` returns; an unknown tool is not evidence that the client should broaden its permissions. |

See [troubleshooting](/guides/troubleshooting#the-mcp-server-rejects-the-agent) for tracing problems that are not specific to MCP.

<Accordion title="Endpoints and protocol details">
  The project-data server is a dynamic Node.js route in Hue's main web application, not a separate data service. `https://app.hue.run/api/mcp` remains a compatibility alias for project keys. Use the canonical URL for browser sign-in: the alias can fail when a client omits the OAuth resource parameter. The `search_hue_docs` tool delegates documentation searches to the Mintlify-hosted documentation endpoint without changing the project-data server's authentication boundary.

  The server is stateless and accepts clients that start with `initialize` (MCP revisions up to 2025-11-25) as well as clients on the stateless 2026-07-28 revision, so clients need no extra flags. It accepts `POST` only. A `GET` with no `Authorization` header gets `401` with the sign-in challenge. A `GET` with a well-formed Hue bearer credential gets `405` with `Allow: POST` without checking whether that credential is valid. A malformed credential still gets `401`. Use the POST key check above to diagnose a failing key. A request carrying `Mcp-Session-Id` gets `404`, because the server keeps no sessions. MCP clients handle these answers themselves.
</Accordion>

## What you can do

Use `list_projects` to discover the available projects. Every other tool works on one selected project within the connection’s access. Write tools need **Read and write** without `?read_only=true`. Ask before any write, and do not auto-approve write tools.

<AccordionGroup>
  <Accordion title="Investigate a failure">
    "Show the error traces from the last 24 hours", "open the slowest span of that trace", "what did the model see before it failed?" (recorded content requires `include_content: true` and the application must have captured it). Tools: `search_traces`, `get_trace`, `get_span`, `list_sessions`. Each trace reports a `state`: `running` while a span is still open, `stalled` when 15 minutes pass without new data and no end arrives, or `ended`. Only the TypeScript SDK 0.7.0 and Python SDK 0.4.0 or later report open spans, and only Hue and AI spans still open when their placeholder is exported, usually about 1.5 s after they start; other spans, including every span from other exporters, appear once they end.
  </Accordion>
  <Accordion title="Confirm delivery">
    "Did trace 0af7651916cd43dd8448eb211c80319c arrive with spans a and b, with model and usage recorded?" Tools: `verify_trace`.
  </Accordion>
  <Accordion title="Trace checks">
    Read the project's production trace checks with `list_trace_checks` and `get_trace_checks`, their results with `get_trace_check_results` (source excerpts require `include_content: true`), and coverage and request timing with `get_trace_check_summary`. Results are advisory, not evaluator scores. With **Read and write**, save a new draft version with `save_trace_checks`, set the separate consent to send recorded content to the trace-check model with `set_trace_checks_consent`, test a saved version on up to ten traces with `test_trace_checks`, turn automatic checks on or off with `activate_trace_checks` and `deactivate_trace_checks`, and queue recent traces for the active version with `reassess_trace_checks`. Through sign-in, only an owner or admin can make these changes.
  </Accordion>
  <Accordion title="Read evaluations">
    "List my eval sets", "show the cases in the latest version of the regression set", "summarize the last run and list the failing cases", "compare it against the baseline run". Tools: `list_eval_sets`, `get_eval_set`, `list_evaluators`, `get_evaluator`, `list_runs`, `get_run`, `list_run_items`, `get_run_item`, `get_run_execution`, `list_scoring_runs`, `get_scoring_run`, `list_environments`, `get_environment`.
  </Accordion>
  <Accordion title="Author evaluations">
    Create an eval set (`add_eval_set`), add cases (`add_eval_set_cases`), edit a draft (`update_eval_set`, `update_eval_set_case`, `delete_eval_set_case`, `add_eval_set_draft`), freeze it (`freeze_eval_set_version`), then pin evaluators (`add_evaluator`, `publish_evaluator`, `copy_evaluator`) and create a run (`add_run`). Record scoring results with `add_scoring_run` and `add_scoring_results`. Hide retired identities with `archive_eval_set`, `archive_evaluator` or `archive_environment`. Case execution can stay with the SDK, a managed run, a local agent, or `start_run_case` / `complete_run_case` / `finish_run`. Never auto-approve `add_*`, `update_*`, `delete_*`, `publish_*`, `freeze_*`, `archive_*` or `copy_*`.
  </Accordion>
  <Accordion title="Author environments">
    `add_environment` and `publish_environment_version` create a simulated world a case can pin.
  </Accordion>
  <Accordion title="Reviewed cases">
    `list_cases` lists cases reviewed from traces, drafts and published, with links. `get_case` returns one case with its eval set, saved version, pinned outcome evaluator, environment and a ready run block for `launch_local_run` or `hue eval`; the reviewed task text is returned only with `include_content: true`.
  </Accordion>
  <Accordion title="Create a case from a trace">
    Requires **Read and write** without `?read_only=true`. In Hue, open the trace and choose **Create case** in the trace inspector, or choose **Create case** from **Suggested cases**. A coding agent runs the same review over MCP. `add_case_conversion` pins the trace into a draft case, and repeating the call with the same idempotency key returns the same draft. `get_case_conversion` reports readiness and findings, and returns the saved task, starting world and criteria only with `include_content: true`. `update_case_conversion` edits the draft, and `publish_case_conversion` publishes a ready draft into a new or existing eval set. `archive_case_conversion` abandons a draft; a published case can't be archived, but its eval set can. Before you publish, review the task and the starting world, and set success criteria independently of the recorded answer. A trace that is still running can't become a case. For a group of traces, `list_suggested_cases` finds suggested groups, `add_case_conversion_batch` starts a batch, `get_case_conversion_batch` reads its progress and `update_case_conversion_batch` retries failed items, cancels a batch or resumes a stalled one. `get_case_divergence` compares a case execution with its source trace. The REST endpoint `https://app.hue.run/api/v1/case-conversions` runs the same workflow. `list_cases` and `get_case` then read the case, whether it is a draft or published.
  </Accordion>
  <Accordion title="Intents">
    Read the taxonomy and buckets with `get_intent_taxonomy`, `get_intent_summary` and `list_intent_traces`. `save_intent_taxonomy` republishes categories and requires an owner or admin.
  </Accordion>
  <Accordion title="Managed and local runs">
    List and inspect with `list_managed_targets`, `preflight_managed_run`, `get_managed_run`, `list_local_agents` and `get_local_run`. `add_managed_target` stores an encrypted credential and requires an owner or admin. Launch or cancel with `launch_managed_run`, `cancel_managed_run` and `launch_local_run`.
  </Accordion>
  <Accordion title="Artifacts and hosted judges">
    `list_artifacts` and `get_artifact` return metadata only; bytes stay on HTTP. `get_judge_budget` reports hosted-judge usage. `add_judge_jobs` enqueues scoring jobs. Review judgments with `list_judge_results` and `get_judge_alignment`; record human labels with `label_judge_result`, promote or demote with `set_judge_promotion`, and check changes with `check_judge_regression`. `set_judge_alignment_bar` changes the promotion requirements and requires an owner or admin.
  </Accordion>
  <Accordion title="Keys, members and settings">
    `list_api_keys` and `list_members` are reads. `add_api_key`, `revoke_api_key` and `update_project` require an owner or admin, and a connected app cannot call `add_api_key`. Wherever a tool requires an owner or admin, Hue checks the current role of the key's creator or of the member who signed in. Minted tokens appear once. Billing and plan changes are not on this server.
  </Accordion>
  <Accordion title="Look things up">
    "How do I flush spans in a Next.js streaming route?" Tools: `search_hue_docs`, `load_hue_guide` (`data-model`, `investigate`, `author`).
  </Accordion>
</AccordionGroup>

Every `trace_id` argument accepts a Hue id, the 32-hex OpenTelemetry trace id or a Hue trace link. Time windows accept ISO-8601 or relative values such as `15m`, `24h` or `7d` (up to 90 days). Paginated tools use opaque cursors. Tools that accept `max_chars` bound their output; truncated results say so and suggest a narrower request. Draft writes take `expected_revision` from `get_eval_set`.

## Content and safety

- Recorded prompts, outputs, tool arguments and raw attributes are omitted unless a tool is called with `include_content: true`. By default the server returns metadata (model, provider, usage, tool names, status, timing) and the names of recorded attributes. Content reads are recorded in the project's audit log.
- Names, generated titles, session keys, metadata and judge explanations come from your application and its end users. They are length-capped, and every result that carries them starts with a notice that they are data, not instructions; recorded content is wrapped in delimited blocks.
- Credential-shaped strings are redacted from results and from documentation queries before those leave Hue. The one exception is `add_api_key`, which returns the newly minted token once so it can be stored; later tools never return it.
- An organization connection can select only active projects in its approved organization. A key or earlier project connection stays within its original project. Resource ids and pasted links never broaden that access or replace the explicit `project_id` selection required by an organization connection.
- Add `?read_only=true` to a key-based URL to hide and reject any write tool regardless of the key. New sign-in approvals use **Read and write**; use a **Read** project key when the credential must be limited to reads.
- Requests are limited to 1 MiB. Ten failed authentications for one key within a minute return `429` with `Retry-After: 60`, and for those 60 seconds the correct key is refused too. A busy server returns `503` with `Retry-After`.

## Retired tool names

Hue served the earlier tool names below as aliases of the current tools for one production release, then retired them. A call to a retired name now fails as an unknown tool. Update prompts, allowlists and approval rules to the current names.

`add_trace_to_eval_set` and its earlier name `add_trace_to_dataset` were removed. To turn a trace into a case, use the case conversion tools, **Create case** in Hue or the `/api/v1/case-conversions` REST endpoint, as described under [what you can do](#what-you-can-do).

<Accordion title="Retired and current tool names">
  | Retired name | Current name |
  | --- | --- |
  | `add_dataset` | `add_eval_set` |
  | `add_dataset_cases` | `add_eval_set_cases` |
  | `add_dataset_draft` | `add_eval_set_draft` |
  | `add_scorer` | `add_evaluator` |
  | `add_experiment` | `add_run` |
  | `add_evaluation_results` | `add_scoring_results` |
  | `add_evaluation_run` | `add_scoring_run` |
  | `archive_dataset` | `archive_eval_set` |
  | `archive_scorer` | `archive_evaluator` |
  | `complete_experiment_case` | `complete_run_case` |
  | `copy_scorer` | `copy_evaluator` |
  | `delete_dataset_case` | `delete_eval_set_case` |
  | `finish_experiment` | `finish_run` |
  | `freeze_dataset_version` | `freeze_eval_set_version` |
  | `get_scenario` | `get_case` |
  | `get_dataset` | `get_eval_set` |
  | `get_scorer` | `get_evaluator` |
  | `get_experiment` | `get_run` |
  | `get_experiment_execution` | `get_run_execution` |
  | `get_experiment_item` | `get_run_item` |
  | `get_evaluation_run` | `get_scoring_run` |
  | `list_scenarios` | `list_cases` |
  | `list_datasets` | `list_eval_sets` |
  | `list_scorers` | `list_evaluators` |
  | `list_experiment_items` | `list_run_items` |
  | `list_experiments` | `list_runs` |
  | `list_evaluation_runs` | `list_scoring_runs` |
  | `publish_scorer` | `publish_evaluator` |
  | `start_experiment_case` | `start_run_case` |
  | `update_dataset` | `update_eval_set` |
  | `update_dataset_case` | `update_eval_set_case` |
</Accordion>
